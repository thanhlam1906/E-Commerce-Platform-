# Ecommerce Project

## Stack
- **Backend:** Java Spring Boot (Product-Catalog-Service)
- **Database:** MongoDB
- **Infrastructure:** Docker, Gradle

## VoltStack Company (ACTIVE)

Dự án này vận hành dưới VoltStack Technologies. Mọi task mới đi qua `/organize`.

### Agent mặc định theo loại việc

| Việc | Dùng agent |
|------|-----------|
| Tính năng mới (backend) | `backend-developer` + `java-architect` + `spring-boot-engineer` |
| API mới / sửa API | `api-designer` → `spring-boot-engineer` |
| Bug fix | `debugger` → `error-detective` |
| Database / query | `postgres-pro` (cho SQL sau này) |
| Docker / deploy | `docker-expert` → `devops-engineer` |
| Test | `test-automator` → `qa-expert` |
| Code review | `code-reviewer` (voltagent-qa-sec) |

### Slash commands dùng trong project

| Command | Dùng khi |
|---------|---------|
| `/organize` | Bắt đầu task mới bất kỳ |
| `/backend` | Backend work (chính của project này) |
| `/qa` | Testing, bug fixing |
| `/devops` | Docker, CI/CD, deploy |

### Ponytail Mode

Code tối thiểu. Không over-engineer. Spring Boot đã có sẵn — tận dụng tối đa những gì framework cung cấp trước khi tự viết.

### Context

- Project path: `D:\project\vibe coding\ecomerce`
- Service: `Product-Catalog-Service` (Spring Boot + Gradle)
- Scripts: `.\gradlew.bat bootRun` để chạy local
